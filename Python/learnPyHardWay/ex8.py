formatter='%r %r %r %r'

print formatter % (
        "I had this thing",
        "That you could type up right",
        "But it didn't sing.", # there is a single quote in this sentence, so the output is "<sentence>", with double quote
        "So I said goodnight"
        )


